# Chat Session Export

- **Session ID**: `d0de220d-4fc7-406f-8e76-f22b20fee0e6`
- **Start Time**: 2026-04-28T06:48:41.929Z
- **Exported**: 2026-04-28T07:05:50.694Z

- **Working Directory**: `/home/daniel/qwencode`
- **Git Repository**: Daniel-Uzcategui/mnemosyne
- **Git Branch**: `master`

- **Model**: qwen3.5-35b-Q6
- **Prompt Count**: 1

- **Total Tokens**: 285415

- **Files Written**: 1
- **Lines Added**: 216
- **Lines Removed**: 30

<details>
<summary><strong>Unique Files Referenced (1)</strong></summary>

- `/home/daniel/qwencode/server.js`
</details>

---

## User

Act as a Staff Inception/Node.js Architect. We need to upgrade our existing "KV Cache Semantic Router" (built with Fastify) to a true Enterprise V2 Architecture. 

The current proxy perfectly handles SSE streaming using `reply.hijack()` and `pump()`, and successfully intercepts requests to hash the system prompt. However, to support background AI agents and massive RAG workloads concurrently, we need to implement 3 new major features in a single, robust `server.js` file.

REQUIREMENTS:

1. Dynamic Slot Load Balancing (Request Queueing)
- llama.cpp is now running with `--parallel 3`.
- Implement a Concurrency Controller (Semaphore/Queue) in Node.js. 
- The proxy must only allow `MAX_CONCURRENT_SLOTS` (set to 3) active requests to llama.cpp at the same time.
- If a 4th request comes in, it MUST wait in a FIFO queue until a slot frees up. Do not drop the request.

2. LRU Garbage Collector (Garbage Sweeper)
- The cache directory is now a RAMDisk mounted at `/home/daniel/Models/ramdisk_cache`. Since RAM is limited, we need an automated sweeper.
- Implement a background `setInterval` (e.g., every 5 minutes) that reads the cache directory.
- Use `fs.stat` to check the `mtime` or `atime` of each `.bin` file.
- Delete any file older than 12 hours to prevent RAM overflow. Log the cleanup operations.

3. Advanced Radix/Prefix Caching (Smart Hashing)
- We are injecting huge RAG manuals. Sometimes they are in the System Prompt, sometimes in the FIRST User message.
- Upgrade the `extractSystemPrompt` and hashing logic:
  - Extract the `system` message.
  - Inspect the FIRST `user` message. If its length is > 1000 characters (indicating injected RAG context), concatenate it with the system prompt BEFORE hashing.
  - The cache key (hash) should represent the entire static context to maximize Cache Hits.

4. PRESERVE EXISTING MAGIC (CRITICAL)
- DO NOT alter the `reply.hijack()` SSE streaming logic. It is perfect.
- Keep using native `fetch`.
- Keep the `FST_ERR_REP_ALREADY_SENT` fix (no `reply.send()` after hijack).
- Keep the graceful degradations and ANSI console logs.

Write the complete, refactored `server.js` file. Think step-by-step about the Concurrency Queue implementation before writing the code to ensure it doesn't block the Node event loop.


## Assistant

The user wants me to upgrade their existing KV Cache Semantic Router server.js with 4 major features. Let me first read the current server.js to understand the existing code, then implement all the changes.

Let me break down what needs to happen:

1. Read current server.js
2. Implement dynamic slot load balancing (semaphore/queue)
3. Implement LRU garbage collector
4. Implement advanced radix/prefix caching (RAG context in first user message)
5. Preserve all existing hijack/SSE streaming logic

Let me start by reading the current file.


### Tool: ReadFile: server.js
**Status**: completed

**Input:**
```json
{
  "file_path": "/home/daniel/qwencode/server.js"
}
```

**Affected Files:**
- `/home/daniel/qwencode/server.js`

**Output:**
```javascript
#!/usr/bin/env node
/**
 * KV Cache Semantic Router
 * ========================
 * Inverse API Gateway for llama.cpp — caches KV state by hashing the
 * System Prompt and saving / restoring per-slot .bin files on disk.
 *
 * Usage:
 *   node server.js
 *
 * Listens on :8080, proxies to llama.cpp at :11434.
 */

'use strict';

// ─── Dependencies ───────────────────────────────────────────────────
// npm install fastify @fastify/cors @fastify/formbody
// (fetch is native in Node ≥ 18)

const { createHash } = require('crypto');
const { existsSync, mkdirSync } = require('fs');
const { join } = require('path');
const fastify = require('fastify');
const fastifyCors = require('@fastify/cors');
const fastifyFormBody = require('@fastify/formbody');

// ─── Configuration ──────────────────────────────────────────────────
const PORT = 8080;
const LLAMA_HOST = '127.0.0.1';
const LLAMA_PORT = 11434;
const LLAMA_BASE = `http://${LLAMA_HOST}:${LLAMA_PORT}`;
const CACHE_DIR = join('/home/daniel', 'Models', 'slot_cache');

// ─── Helpers ────────────────────────────────────────────────────────

// ANSI colour helpers for terminal output
const C = {
  reset:  '\x1b[0m',
  bold:   '\x1b[1m',
  grey:   '\x1b[90m',
  green:  '\x1b[32m',
  yellow: '\x1b[33m',
  red:    '\x1b[31m',
  cyan:   '\x1b[36m',
  blue:   '\x1b[34m',
};

function log(tag, msg) {
  console.log(`${C.grey}${new Date().toISOString().slice(11, 23)}${C.reset}  ${C.bold}[${tag}]${C.reset} ${msg}`);
}

function logHit(hash) {
  console.log(
    `${C.green}${C.bold}■ CACHE HIT${C.reset}  slot  →  ${hash}.bin  (restoring from disk)`
  );
}

function logMiss(hash) {
  console.log(
    `${C.yellow}${C.bold}■ CACHE MISS${C.reset}  slot  →  ${hash}.bin  (will save after response)`
  );
}

function logErr(msg) {
  console.error(`${C.red}${C.bold}✖ ERROR${C.reset}   ${msg}`);
}

// ─── Ensure cache directory exists ──────────────────────────────────
if (!existsSync(CACHE_DIR)) {
  mkdirSync(CACHE_DIR, { recursive: true });
  log('INIT', `Created cache directory: ${CACHE_DIR}`);
}

// ─── Fastify server ─────────────────────────────────────────────────
const server = fastify({
  bodyLimit: 50 * 1024 * 1024,   // 50 MB
  maxParamLength: 1024 * 1024,
  disableRequestLogging: true,
});

server.register(fastifyCors, { origin: true });
server.register(fastifyFormBody);

// ─── Core logic ─────────────────────────────────────────────────────

/**
 * Extract the System Prompt and User Messages from an OpenAI-format
 * /v1/chat/completions request body.
 */
function extractSystemPrompt(body) {
  const messages = Array.isArray(body?.messages) ? body.messages : [];
  let systemPrompt = '';
  const userMessages = [];

  let systemFound = false;
  for (const msg of messages) {
    if (!systemFound && msg.role === 'system' && msg.content) {
      systemPrompt = typeof msg.content === 'string'
        ? msg.content
        : JSON.stringify(msg.content);
      systemFound = true;
    } else {
      userMessages.push(msg);
    }
  }

  return { systemPrompt, userMessages };
}

function sha256(str) {
  return createHash('sha256').update(str, 'utf8').digest('hex');
}

/**
 * POST helper using native fetch (Node ≥ 18).
 */
async function llamaPost(path, payload) {
  const res = await fetch(`${LLAMA_BASE}${path}`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(payload),
  });

  if (!res.ok) {
    const text = await res.text().catch(() => '(no body)');
    throw new Error(
      `llama.cpp ${path} failed ${res.status}: ${text.slice(0, 300)}`
    );
  }

  return res;
}

function sleep(ms) {
  return new Promise((resolve) => setTimeout(resolve, ms));
}

/**
 * Save the KV cache for slot 0 to disk (background, fire-and-forget).
 */
async function saveKVCache(hash, model) {
  const filename = `${hash}.bin`;

  try {
    log('SAVE', `Starting  →  ${filename}`);

    const res = await llamaPost('/slots/0?action=save', { filename, model });

    const bodyText = await res.text().catch(() => '');
    let savedFile = filename;
    try {
      const parsed = JSON.parse(bodyText);
      if (parsed?.path) savedFile = parsed.path;
    } catch { /* ignore */ }

    log('SAVE', `${C.green}Success${C.reset}  →  ${savedFile}  (${res.status})`);
  } catch (err) {
    logErr(`Save failed for ${hash}.bin: ${err.message}`);
  }
}

/**
 * Restore the KV cache for slot 0 from disk.
 */
async function restoreKVCache(hash, model) {
  const filename = `${hash}.bin`;

  try {
    log('RESTORE', `Loading  →  ${filename}`);

    const res = await llamaPost('/slots/0?action=restore', { filename, model });

    log('RESTORE', `${C.green}Success${C.reset}  →  ${filename}  (${res.status})`);
  } catch (err) {
    logErr(`Restore failed for ${hash}.bin: ${err.message}`);
    throw err;
  }
}

/**
 * SSE streaming proxy via hijack.
 */
async function proxyStream(llamaRes) {
  reply.hijack();

  reply.raw.writeHead(llamaRes.status, {
    'Content-Type':    'text/event-stream',
    'Cache-Control':   'no-cache',
    'Connection':      'keep-alive',
    'X-Accel-Buffering': 'no',
  });

  const reader = llamaRes.body?.getReader();
  if (reader) {
    try {
      while (true) {
        const { done, value } = await reader.read();
        if (done) break;
        reply.raw.write(value);
      }
    } catch {} finally {
      reader.releaseLock();
      reply.raw.end();
    }
  } else {
    reply.raw.end();
  }
}

// ─── Route handler ──────────────────────────────────────────────────
server.post('/v1/chat/completions', async (request, reply) => {
  const body = request.body;
  const reqId = (body?.metadata && body.metadata?.request_id)
    || body?.id
    || '????';

  // 1. Extract system prompt
  const { systemPrompt, userMessages } = extractSystemPrompt(body);
  const hash = systemPrompt ? sha256(systemPrompt) : null;
  const model = body?.model || null;

   // No system prompt → direct proxy
  if (!hash) {
    log('PROXY', `No system prompt — forwarding directly (id=${reqId})`);

    const llamaRes = await fetch(`${LLAMA_BASE}/v1/chat/completions`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Host': `${LLAMA_HOST}:${LLAMA_PORT}`,
      },
      body: JSON.stringify(body),
    });

    await proxyStream(llamaRes);
    return;
  }

  const filename = `${hash}.bin`;
  const cacheHit = existsSync(join(CACHE_DIR, filename));

  // ──────────────────────────────────────────────────────────────
  // CACHE HIT — restore KV cache, then proxy with user messages only
  // ──────────────────────────────────────────────────────────────
  if (cacheHit) {
    logHit(hash);

    try {
      await restoreKVCache(hash, model);
      log('INFO', `KV cache restored for ${hash.slice(0, 12)}...`);
    } catch (err) {
      log('WARN', `Restore failed (${err.message}) — falling back to full request`);
    }

    // Proxy with only user messages (system prompt already in KV cache)
    const proxyBody = { ...body, messages: userMessages };
    const llamaRes = await fetch(`${LLAMA_BASE}/v1/chat/completions`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Host': `${LLAMA_HOST}:${LLAMA_PORT}`,
      },
      body: JSON.stringify(proxyBody),
    });

    reply.raw.writeHead(llamaRes.status, {
      'Content-Type':    'text/event-stream',
      'Cache-Control':   'no-cache',
      'Connection':      'keep-alive',
      'X-Accel-Buffering': 'no',
      'X-Cache-Status':  'HIT',
    });

    const reader = llamaRes.body?.getReader();
    if (reader) {
      try {
        while (true) {
          const { done, value } = await reader.read();
          if (done) break;
          reply.raw.write(value);
        }
      } catch {} finally {
        reader.releaseLock();
        reply.raw.end();
      }
    } else {
      reply.raw.end();
    }
    return;
  }

  // ──────────────────────────────────────────────────────────────
  // CACHE MISS — proxy full request, save KV cache after stream
  // ──────────────────────────────────────────────────────────────
  logMiss(hash);

  const llamaRes = await fetch(`${LLAMA_BASE}/v1/chat/completions`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Host': `${LLAMA_HOST}:${LLAMA_PORT}`,
    },
    body: JSON.stringify(body),
  });

  if (!llamaRes.ok) {
    const errText = await llamaRes.text().catch(() => '');
    logErr(`llama.cpp returned ${llamaRes.status}: ${errText.slice(0, 500)}`);
    throw new Error(`llama.cpp returned ${llamaRes.status}: ${errText.slice(0, 300)}`);
  }

  reply.hijack();
  reply.raw.writeHead(llamaRes.status, {
    'Content-Type':    'text/event-stream',
    'Cache-Control':   'no-cache',
    'Connection':      'keep-alive',
    'X-Accel-Buffering': 'no',
    'X-Cache-Status':  'MISS',
  });

  const reader = llamaRes.body?.getReader();

  if (reader) {
    const pump = async () => {
      try {
        while (true) {
          const { done, value } = await reader.read();
          if (done) break;
          reply.raw.write(value);
        }
      } catch {} finally {
        reader.releaseLock();
        reply.raw.end();
      }
    };

    // Save KV cache after stream is fully consumed
    (async () => {
      await pump();
      await saveKVCache(hash, model);
    })();
  } else {
    reply.raw.end();
    setImmediate(() => saveKVCache(hash, model));
  }

  return;
});

// ─── Error handler ──────────────────────────────────────────────────
server.setErrorHandler((error, request, reply) => {
  logErr(`${error.message}  [${request.id || 'no-id'}]`);
  if (!reply.sent) {
    reply.status(500).send({
      error: {
        message: error.message,
        type: 'internal_error',
      },
    });
  }
});

// ─── Startup ────────────────────────────────────────────────────────
const start = async () => {
  try {
    await server.listen({ port: PORT, host: '0.0.0.0' });

    console.log(`
${C.bold}${C.cyan}╔═══════════════════════════════════════════════════════════╗${C.reset}
${C.bold}${C.cyan}║${C.reset}  ${C.bold}${C.green}KV Cache Semantic Router${C.reset}                                ${C.bold}${C.cyan}║${C.reset}
${C.bold}${C.cyan}║${C.reset}  Listening on  ${C.bold}http://0.0.0.0:${PORT}${C.reset}                      ${C.bold}${C.cyan}║${C.reset}
${C.bold}${C.cyan}║${C.reset}  Proxy target  ${C.bold}${LLAMA_BASE}${C.reset}                                    ${C.bold}${C.cyan}║${C.reset}
${C.bold}${C.cyan}║${C.reset}  Cache dir     ${C.bold}${CACHE_DIR}${C.reset}                                    ${C.bold}${C.cyan}║${C.reset}
${C.bold}${C.cyan}╚═══════════════════════════════════════════════════════════╝${C.reset}
`);
  } catch (err) {
    logErr(`Cannot start server: ${err.message}`);
    process.exit(1);
  }
};

start();

```


## Assistant

Now I have the full picture. Let me plan the implementation carefully:

1. **Dynamic Slot Load Balancing** - A semaphore-based queue. I need a FIFO queue that tracks active slots (max 3). When a request comes in:
   - If slots < MAX, proceed immediately
   - If slots >= MAX, enqueue the request and wait for a slot to free up
   - When a slot finishes (stream ends), dequeue and start the next request

2. **LRU Garbage Collector** - Background interval that scans the cache dir, checks mtime, deletes files older than 12 hours.

3. **Advanced RAG Hashing** - Extract system prompt, then check the first user message. If it's > 1000 chars, concatenate with system prompt before hashing.

4. **Preserve hijack SSE streaming** - Don't change the streaming logic.

Let me think through the concurrency queue carefully. The tricky part is that each request to llama.cpp streams back via hijack, and we need to track when that stream ends so we can release the slot. The queue needs to hold a reference to the full request context (body, reply, hash, model, reqId) so that when a slot becomes available, we can replay the entire handler logic for that request.

I'll use a semaphore pattern with a FIFO queue where each slot tracks its completion promise. When a request needs a slot, it waits for one to free up, then proceeds with the llama.cpp call and the streaming proxy. Once the stream finishes, the slot releases and the next queued request can proceed.

The tricky part is tracking when the hijacked stream ends so I can signal slot completion. I'm wrapping the slot acquisition in a wrapper function that handles the queue logic, then I'll need a way to detect stream termination—either by listening for the 'close' event on the raw socket or by wrapping the reader's releaseLock to signal when the slot is free.

Actually, I think the cleanest approach is to make the slot release happen after the stream fully completes. I'll create a slot manager with a queue that manages concurrent access, then refactor the route handler to acquire a slot before proxying to llama.cpp and release it once streaming finishes.

The slot manager uses a FIFO queue where each item holds the request body, reply object, hash, model, and request ID. When a request comes in, it acquires a slot (waiting in the queue if all are busy), then proxies the request and releases the slot when done.

Building the slot manager... Writing the queue logic...

Now I'm thinking through the LRU sweeper — it'll run every 5 minutes and delete cache files older than 12 hours, just keeping it straightforward. For the RAG enhancement, I need to extract the system prompt first, then check if the first user message is large enough to warrant concatenation before hashing, which should improve cache hit rates.

Let me put together the complete refactored server with all these pieces integrated.

Writing the full server implementation... Finishing the slot acquisition logic...

Now I need to handle the cache hit path differently—when there's a cache hit, I restore the KV cache before proxying, but I still need to track the slot usage even though the restoration happens before the request enters the queue.

Actually, I should keep the slot tracking consistent across all paths. The cache restoration is quick, so it makes sense to do it outside the slot queue to avoid blocking other requests unnecessarily. But then I need to make sure the slot is properly acquired and released for the actual proxying phase, regardless of whether we hit the cache or not.

I'm restructuring the handler to acquire a slot first, then attempt the cache restore if needed, and finally proxy the request through llama.cpp. This way the slot lifecycle is clean and uniform across all code paths.

Now I need to wire up the slot manager to call release() when streaming completes. I'm thinking through the best way to detect when the stream finishes—whether that's through the 'close' event on the raw socket or by wrapping the reader's releaseLock method.

Wrapping the reader's releaseLock method...

I'm setting up a slot manager that wraps the handler with queueing logic, so requests get queued when slots are full and released when streaming completes.

Now I need to handle the cache-hit path carefully — when we restore from cache, we proxy with user messages only, but the slot still needs to be released when that stream finishes. I'm tracing through both paths to make sure the slot release happens consistently, whether it's a cache hit or miss.

The key insight is that the slot should be acquired before any work begins, and released only after the streaming proxy completes. This way, if the cache restore fails and we fall back to the full request, the slot is already held and ready to go. The restore operation happens quickly, so blocking on it while holding a slot is acceptable.

Now I'm thinking through the edge cases — what happens if the queue times out or if the client disconnects before their request even reaches the front of the line?

For client disconnects, I can listen for the 'close' event on the raw socket and reject the queued promise if the connection drops. For queue timeouts, I'll add a configurable timeout so stuck requests don't hang indefinitely.

I'm also reconsidering the stream release mechanism — the original code has some try/catch blocks that swallow errors, which is fine for robustness but I need to make sure the slot release happens reliably even when errors occur.

Wrapping releaseLock to ensure slot cleanup...

Now I'm thinking through the LRU sweeper implementation — it'll scan the cache directory and check file modification times to identify which cached slots are stale and can be removed. I'm setting up the cache directory check and then initializing the Fastify server with the necessary plugins for CORS and form body parsing. Now I'm defining the core logic functions—extracting system prompts and user messages from the request body, hashing strings with SHA256, and making POST requests to the llama.cpp API.

Building the KV cache functions... Writing the streaming proxy handler...

Now I'm setting up the main route handler that extracts the system prompt and user messages from the request, computes a hash for caching purposes, and determines whether a cache hit or miss occurred by checking if the corresponding binary file exists in the cache directory.

For a cache hit, I restore the KV cache from disk if the file exists, then proxy the request with only the user messages since the system prompt is already loaded into the cache. I'm setting up the response headers to indicate this is a cache hit and begin streaming the SSE response back to the client.

Writing the stream handler...

For cache misses, I proxy the full request to llama.cpp and stream the response back, logging the miss for debugging purposes.

Then I set up a background task that reads through the SSE stream, forwards each chunk to the client, and once the stream completes, saves the KV cache for that hash. If there's no stream body, I just end the response and trigger the save immediately.

Now I'm wrapping this entire handler in a slot manager to handle concurrent request limits, and setting up an error handler to catch and log any issues that occur during request processing.

I need to think through the slot manager logic more carefully—specifically how requests should be queued when all slots are in use, and whether the slot should be acquired before or after checking the cache. If I acquire the slot first, a request with a cache hit still holds a slot while we're doing the restore operation, which could block other requests unnecessarily.

Actually, the restore is just an HTTP call to llama.cpp, so it's already using network time. The real question is whether I should queue requests before or after the cache check. If I queue after the cache check, a cache hit request might slip through while a cache miss request waits—that feels unfair. But if I queue before the cache check, the cache hit request holds a slot during the restore, which is also wasteful.

I think the fairer approach is to queue first, then check the cache. This way, all requests are ordered consistently, and the slot is released once the entire operation (including the restore for cache hits) completes.

Now I need to handle the case where the client disconnects while waiting in the queue. I'll set up a listener for the 'close' event on the reply object to reject the slot promise if the client disconnects before their turn.

Implementing the queue timeout logic...

Now I'm thinking through the race condition with the `released` flag — when a stream ends and we try to release the slot, there's a window where the slot could be acquired by another request before we actually signal the release. I need to make sure the release happens atomically with the slot acquisition to prevent that gap.

Actually, looking at the flow more carefully: the queue processes sequentially, so when `slot.release()` is called and triggers `processQueue()`, that function runs to completion before the next queued request can proceed. The `resolve()` call doesn't yield control back until after the queue has moved forward, so the race condition I was worried about doesn't actually happen.

Now I'm realizing there's a potential issue with the `released` flag in the error path. If an error occurs during the request and we call `slot.release()`, but then an exception escapes the handler, the flag might not get set properly. I need to wrap the slot acquisition in a try-finally block to ensure `released` is always set to true, even if something fails.

The key insight is that `processQueue()` runs synchronously to completion—it grabs the next queued item, acquires a slot, and returns without yielding. So when `processQueue()` finishes and returns to `acquireSlot()`, we're guaranteed that either we got a slot or the queue is empty. This means the `released` flag will be correctly set before we return from `release()`, preventing any race conditions.

Now I'm thinking through the edge case where `processQueue()` tries to dequeue but finds the queue empty—that just means there's nothing left to process, so we safely return without resolving anything. And if `processQueue()` successfully grabs an item and resolves the promise, that item is no longer in the queue, so we won't encounter it again.

For the LRU sweeper, I'm realizing there's a potential issue with the file I/O operations running in the background. Since `fs.readdir` and `fs.stat` are asynchronous, multiple calls could interleave and cause problems with the queue or file state.

Actually, looking at this more carefully, the sweeper is just reading and deleting files—it doesn't modify any shared state like the queue. So concurrent runs are fine; they'll just process different batches of files without conflicts.

Now I'm thinking through the RAG context detection. The requirement is to check if the first user message exceeds 1000 characters and concatenate it with the system prompt before hashing. But I need to be careful about where to extract the RAG context from—should I only look at the first user message, or check all user messages?

Also, I'm realizing there's a subtle issue with how I'm handling the cache hit scenario. When I detect RAG context in the first user message, I'm including it in the hash for the cache miss path, but then removing it from the messages sent to llama on cache hit. This means llama receives only the remaining user messages after the system prompt, which is correct since the RAG context is already baked into the KV cache from the initial full request.

Actually, I need to reconsider the flow here. On cache hit, I restore the KV cache (which includes the system prompt plus any RAG context that was in the first user message), then send only the remaining user messages to llama. That's the right behavior. On cache miss, I send the full request with everything included.

Now I'm thinking through the edge cases around empty strings and null values. If there's no system prompt, the hash is null and I bypass caching entirely, which is correct. If the system prompt is empty but there's a RAG context in the first user message, I'd still get a hash and attempt caching, which could work. But if both are missing, the hash stays null and I skip caching.

Writing the complete server implementation... Writing the slot manager... Writing the cache sweeper... Writing Fastify server setup... Writing KV cache functions... Building the proxy stream function... Building the route handler... Writing streaming proxy logic... Setting up error handling and server startup...

I'm verifying the slot manager integration works correctly across all three paths—cache hits, cache misses, and direct proxies all acquire a slot before calling the handler and release it afterward. Now I need to make sure the queue timeout is properly configured to reject requests that wait too long.

I'm also thinking through the RAG context extraction logic. The current approach concatenates the system prompt with the first user message, but I'm realizing there's a subtle issue: if the system prompt is empty or missing, I'd still be hashing the RAG context from the first user message, which might not be the intended behavior.

Actually, looking at it more carefully, this seems correct. The RAG context is static data that should be part of the cache key regardless of whether there's a system prompt, so hashing them together makes sense for cache hits.

But I need to double-check the cache hit flow. When restoring from cache, I'm sending only the user messages (excluding the system prompt and RAG context) to llama, which should be right since both are already baked into the KV cache.

Now I'm thinking about the RAG context preservation. If there are multiple user messages and only the first one contains RAG data, I need to make sure I'm stripping out just that first message when constructing the proxy body for the cache hit case.

Looking at the implementation, `userMessages` already excludes the system prompt but includes all user messages, so removing the first one gives me exactly what I need for the proxy request.

I'm noticing a potential issue with empty strings in the hashing logic—if the system prompt is empty but there's RAG context, I'd be hashing just the RAG context, which could work but might not match the intended behavior. Let me trace through the extraction logic to see how empty strings are handled. I'm verifying the logic flow for the RAG context handling. When a cache hit occurs, the KV cache contains both the system prompt and RAG context from the initial save, so sending only the remaining user messages is correct. I'm also confirming that `extractSystemPrompt` properly handles the case where the system prompt appears after other messages by only capturing the first system message and skipping any subsequent ones.

Now I'm checking the edge case where the first user message is empty or lacks content—the length check would correctly skip it since empty strings have zero length. I'm also verifying that the `requestId` property exists on the reply object in Fastify v4, which it does. The implementation is solid and ready to finalize.

For the `bodyLimit` setting, I'm considering whether to increase it from 50MB to 100MB to accommodate large RAG payloads, though the user didn't explicitly request this change. I'll keep it at 50MB since that's already quite generous and the user can adjust it if needed. Now I'm ready to write the final implementation.

I should also verify the LRU sweeper handles edge cases properly—if the cache directory is empty or missing, the sweeper gracefully returns without errors. The implementation looks solid, so I'll add a startup log message for the sweeper and write out the complete file.


### Tool: WriteFile
**Status**: in_progress

**Input:**
```json
{}
```



## Assistant

Now I have the full picture. Let me plan the implementation carefully:

1. **Dynamic Slot Load Balancing (Request Queueing)**: A semaphore-based queue with MAX_CONCURRENT_SLOTS = 3. Each slot is an async counter. When it hits 0, incoming requests queue up. When a request completes, the next in line gets the slot.

2. **LRU Garbage Collector**: A background `setInterval` that scans the cache directory, checks file ages, and deletes files older than 12 hours.

3. **Advanced Radix/Prefix Caching**: Extract the system prompt, check the first user message. If it's > 1000 chars, concatenate it with the system prompt before hashing.

4. **Preserve existing hijack/SSE streaming**: Keep the `reply.hijack()` pattern intact.

Let me think through the concurrency queue carefully:

**Semaphore/Queue Design:**
- `MAX_CONCURRENT_SLOTS = 3`
- `currentSlots = 0`
- `queue = []` (FIFO queue of { resolve, reject } functions)
- `acquire()`: if `currentSlots < MAX`, increment and return immediately. Otherwise, push a promise to the queue and return a promise that resolves when a slot opens.
- `release()`: if queue has items, shift one and resolve it. Otherwise, decrement `currentSlots`.

This is a classic async semaphore pattern. It won't block the event loop because it's all promise-based.

**Garbage Collector:**
- Run every 5 minutes
- Read directory, filter `.bin` files
- Use `fs.stat` to check `mtime`
- Delete files older than 12 hours
- Log operations

**Advanced Hashing:**
- Extract system prompt as before
- Check first user message length
- If > 1000 chars, concatenate system + user message content before hashing
- This gives us a "prefix hash" that represents the full static context

Let me also think about the cache hit/restore logic. Currently, on cache hit, it restores the KV cache then proxies with only user messages. This is correct behavior that I need to preserve.

Now let me write the complete refactored server.js.

Key changes:
1. Add semaphore/concurrency controller
2. Add garbage collector interval
3. Upgrade extractSystemPrompt to also return the first user message for RAG detection
4. Upgrade the hashing logic to concatenate RAG context
5. Keep all existing hijack/streaming logic intact

Let me write this out carefully.


### Tool: TodoWrite
**Status**: completed

**Output:**
```
- [ ] Implement Dynamic Slot Load Balancing (Semaphore/Queue)
- [ ] Implement LRU Garbage Collector (setInterval sweeper)
- [ ] Implement Advanced RAG-aware Hashing (prefix caching)
- [ ] Preserve existing hijack/SSE streaming logic
- [ ] Write complete refactored server.js
```


## Assistant

Let me now implement the complete server.js with all four features. I'll think through the concurrency queue carefully first.

**Concurrency Queue Design:**

```javascript
const MAX_CONCURRENT_SLOTS = 3;
let currentSlots = 0;
const queue = []; // FIFO: each item is { resolve, reject }

function acquire() {
  return new Promise((resolve, reject) => {
    if (currentSlots < MAX_CONCURRENT_SLOTS) {
      currentSlots++;
      resolve();
    } else {
      queue.push({ resolve, reject });
    }
  });
}

function release() {
  if (queue.length > 0) {
    const { resolve } = queue.shift();
    resolve();
  } else {
    currentSlots--;
  }
}
```

This is clean and non-blocking. All waiting is promise-based.

**Garbage Collector:**
- Every 5 minutes, scan CACHE_DIR
- For each .bin file, stat it, check mtime
- Delete if older than 12 hours
- Log what was deleted and how much RAM freed

**RAG-aware Hashing:**
- Extract system prompt
- Get first user message
- If first user message > 1000 chars, concatenate: `systemPrompt + '\n\n' + firstUserContent`
- Hash the combined string
- On cache hit, still restore KV cache, then proxy with user messages only (the system prompt is already baked into the KV cache)

Wait, I need to think about this more carefully. The RAG context is in the first user message. If we hash the combined system + RAG context, then:
- Cache HIT: restore KV cache (which has system + RAG baked in), proxy with user messages (excluding the RAG part?)
- Cache MISS: proxy full request (system + RAG + user), save KV cache after

Actually, the KV cache from llama.cpp's `/slots/0?action=save` captures the entire tokenized state after processing all messages. So if we restore from cache, we're restoring the state AFTER processing system + RAG context. Then we send only the actual user query (without the RAG prefix) to continue generation.

But wait - the current code on cache hit sends `userMessages` (all messages except system). That includes the RAG message. So we'd be sending the RAG context again to llama.cpp, which would double-process it.

Hmm, but the KV cache restore happens BEFORE we send the request. So llama.cpp has the system + RAG state loaded, and then we send the user messages. If the user messages include the RAG context, llama.cpp will process it again.

I think the correct behavior on cache hit should be:
1. Restore KV cache (system + RAG baked in)
2. Send only the actual user query messages (excluding the RAG prefix message)

But that's a behavioral change that might break things. The user said "PRESERVE EXISTING MAGIC" - so I should keep the current behavior where on cache hit, we send `userMessages` (all non-system messages).

Actually, looking at this more carefully - the KV cache represents the model's internal state after processing tokens. If we restore the KV cache for system + RAG, and then send the same user messages (including RAG), llama.cpp will re-process the RAG tokens. This is wasteful but not incorrect - the KV cache will just have the RAG tokens in it twice.

Actually no, that's wrong. The KV cache represents the state at a point in time. When we restore it, we're setting the model's state to "I've processed these tokens up to this point." Then when we send new tokens, the model continues from that point. If we send the RAG tokens again, the model will process them as new tokens, creating new KV entries for them. This is wasteful but not broken.

For now, I'll preserve the existing behavior on cache hit (send all userMessages) since the user said to preserve existing magic. The hashing just needs to be smarter about what gets hashed.

Let me also think about what happens on cache MISS with RAG:
1. We hash system + RAG context → hash
2. We proxy the full request to llama.cpp (system + RAG + user query)
3. After streaming completes, we save the KV cache
4. The saved KV cache has the state after processing system + RAG + user query

Next time, if the same system + RAG context comes in:
1. We hash system + RAG → same hash
2. Cache HIT → restore KV cache (state after system + RAG + user query)
3. Send userMessages (which includes RAG) to llama.cpp

Hmm, this means the cache stores the state after the full conversation including the user query. That's not ideal for a pure prefix cache. But again, the user said to preserve existing behavior.

Actually, let me re-read the original code. On cache miss, it saves the KV cache after the response. The KV cache at that point includes everything that was processed. On cache hit, it restores and then sends user messages. So the cache effectively stores "system + everything up to this point."

For the RAG upgrade, I think the intent is:
- Hash = hash(system + RAG context) — this represents the "static prefix"
- On cache HIT: restore KV cache (system + RAG baked in), send only the actual query
- On cache MISS: proxy full request, save KV cache after

But the current code sends `userMessages` on cache hit, which includes the RAG message. To make this truly efficient, on cache hit we should send only messages AFTER the RAG message.

However, the user said to preserve existing behavior. Let me just focus on the hashing change and keep the message handling the same. The user can always refine this later.

Actually, let me re-read the requirement:
> Extract the `system` message.
> Inspect the FIRST `user` message. If its length is > 1000 characters (indicating injected RAG context), concatenate it with the system prompt BEFORE hashing.
> The cache key (hash) should represent the entire static context to maximize Cache Hits.

This is purely about the hashing logic. The rest of the flow stays the same. Let me implement exactly this.

Now let me write the complete file.

For the RAG-aware extraction, I'll modify `extractSystemPrompt` to return:
- `systemPrompt`: the system prompt string
- `firstUserMessage`: the first user message (or null)
- `userMessages`: all messages except system (for proxying)

Then the hash is:
- If firstUserMessage exists and content > 1000 chars: hash(systemPrompt + '\n\n' + firstUserContent)
- Otherwise: hash(systemPrompt)

Let me write the complete server.js now.


### Tool: TodoWrite
**Status**: completed

**Output:**
```
- [-] Implement Dynamic Slot Load Balancing (Semaphore/Queue)
- [ ] Implement LRU Garbage Collector (setInterval sweeper)
- [ ] Implement Advanced RAG-aware Hashing (prefix caching)
- [ ] Preserve existing hijack/SSE streaming logic
- [ ] Write complete refactored server.js
```


## Assistant

Now let me write the complete server.js with all features implemented.


## Assistant

Now I'll write the complete refactored `server.js` with all four features. The concurrency queue uses a promise-based semaphore — no blocking, pure async.


### Tool: WriteFile: Writing to server.js
**Status**: completed

**Input:**
```json
{
  "content": "#!/usr/bin/env node\n/**\n * KV Cache Semantic Router — Enterprise V2\n * =========================================\n * Inverse API Gateway for llama.cpp with:\n *   • Dynamic Slot Load Balancing (FIFO concurrency queue)\n *   • LRU Garbage Collector (RAMDisk sweeper)\n *   • Advanced RAG-aware Prefix Hashing\n *   • reply.hijack() SSE streaming (preserved)\n *\n * Usage:\n *   node server.js\n *\n * Listens on :8080, proxies to llama.cpp at :11434.\n */\n\n'use strict';\n\n// ─── Dependencies ───────────────────────────────────────────────────\n// npm install fastify @fastify/cors @fastify/formbody\n// (fetch is native in Node ≥ 18)\n\nconst { createHash } = require('crypto');\nconst { existsSync, mkdirSync, statSync, readdirSync, rmSync } = require('fs');\nconst { join, extname } = require('path');\nconst fastify = require('fastify');\nconst fastifyCors = require('@fastify/cors');\nconst fastifyFormBody = require('@fastify/formbody');\n\n// ─── Configuration ──────────────────────────────────────────────────\nconst PORT = 8080;\nconst LLAMA_HOST = '127.0.0.1';\nconst LLAMA_PORT = 11434;\nconst LLAMA_BASE = `http://${LLAMA_HOST}:${LLAMA_PORT}`;\nconst CACHE_DIR = join('/home/daniel', 'Models', 'slot_cache');\n\n// Dynamic Slot Load Balancing\nconst MAX_CONCURRENT_SLOTS = 3;\n\n// LRU Garbage Collector\nconst GARBAGE_COLLECT_INTERVAL_MS = 5 * 60 * 1000;  // 5 minutes\nconst MAX_FILE_AGE_MS = 12 * 60 * 60 * 1000;        // 12 hours\n\n// RAG-aware prefix caching\nconst RAG_CONTENT_LENGTH_THRESHOLD = 1000;\n\n// ─── Helpers ────────────────────────────────────────────────────────\n\n// ANSI colour helpers for terminal output\nconst C = {\n  reset:  '\\x1b[0m',\n  bold:   '\\x1b[1m',\n  grey:   '\\x1b[90m',\n  green:  '\\x1b[32m',\n  yellow: '\\x1b[33m',\n  red:    '\\x1b[31m',\n  cyan:   '\\x1b[36m',\n  blue:   '\\x1b[34m',\n};\n\nfunction log(tag, msg) {\n  console.log(`${C.grey}${new Date().toISOString().slice(11, 23)}${C.reset}  ${C.bold}[${tag}]${C.reset} ${msg}`);\n}\n\nfunction logHit(hash) {\n  console.log(\n    `${C.green}${C.bold}■ CACHE HIT${C.reset}  slot  →  ${hash}.bin  (restoring from disk)`\n  );\n}\n\nfunction logMiss(hash) {\n  console.log(\n    `${C.yellow}${C.bold}■ CACHE MISS${C.reset}  slot  →  ${hash}.bin  (will save after response)`\n  );\n}\n\nfunction logWarn(msg) {\n  console.log(\n    `${C.yellow}${C.bold}⚠ WARN${C.reset}    ${msg}`\n  );\n}\n\nfunction logErr(msg) {\n  console.error(`${C.red}${C.bold}✖ ERROR${C.reset}   ${msg}`);\n}\n\n// ─── Ensure cache directory exists ──────────────────────────────────\nif (!existsSync(CACHE_DIR)) {\n  mkdirSync(CACHE_DIR, { recursive: true });\n  log('INIT', `Created cache directory: ${CACHE_DIR}`);\n}\n\n// ─── 1. Dynamic Slot Load Balancing (Concurrency Queue) ─────────────\n//\n// Promise-based FIFO semaphore. Non-blocking — all waiting happens\n// via microtask resolution, never blocks the Node event loop.\n//\n//   acquire() → returns a Promise that resolves when a slot opens\n//   release() → frees a slot, wakes the next waiter in line\n//\nlet currentSlots = 0;\nconst slotQueue = []; // FIFO queue of { resolve, reject, reqId }\n\n/**\n * Acquire a concurrency slot. If all MAX_CONCURRENT_SLOTS are in\n * use, the caller's promise is queued and resolved later when a\n * slot frees up.\n */\nfunction acquireSlot(reqId) {\n  return new Promise((resolve, reject) => {\n    if (currentSlots < MAX_CONCURRENT_SLOTS) {\n      currentSlots++;\n      log('SLOT', `${C.green}ACQUIRED${C.reset}  (${currentSlots}/${MAX_CONCURRENT_SLOTS})  ${reqId}`);\n      resolve();\n    } else {\n      log('SLOT', `${C.yellow}QUEUED${C.reset}  queue=${slotQueue.length + 1}  (${currentSlots}/${MAX_CONCURRENT_SLOTS})  ${reqId}`);\n      slotQueue.push({ resolve, reject, reqId });\n    }\n  });\n}\n\n/**\n * Release a concurrency slot. If there are waiters in the FIFO\n * queue, the next one gets the slot immediately. Otherwise, just\n * decrement the counter.\n */\nfunction releaseSlot(reqId) {\n  if (slotQueue.length > 0) {\n    const { resolve } = slotQueue.shift();\n    currentSlots++;\n    log('SLOT', `${C.green}PASSED${C.reset}  queue=${slotQueue.length}  (${currentSlots}/${MAX_CONCURRENT_SLOTS})  ${reqId}`);\n    resolve();\n  } else {\n    currentSlots--;\n    log('SLOT', `${C.green}RELEASED${C.reset}  (${currentSlots}/${MAX_CONCURRENT_SLOTS})  ${reqId}`);\n  }\n}\n\n// ─── 2. LRU Garbage Collector (Background Sweeper) ─────────────────\n\nlet gcStats = { totalScanned: 0, totalDeleted: 0, totalBytesFreed: 0 };\n\nfunction garbageCollect() {\n  log('GC', `${C.bold}Starting sweep${C.reset}  (max age=${(MAX_FILE_AGE_MS / 3600000).toFixed(0)}h)`);\n\n  try {\n    const files = readdirSync(CACHE_DIR).filter(f => extname(f) === '.bin');\n    const now = Date.now();\n    let scanned = 0;\n    let deleted = 0;\n    let bytesFreed = 0;\n\n    for (const file of files) {\n      scanned++;\n      const filePath = join(CACHE_DIR, file);\n\n      try {\n        const stat = statSync(filePath);\n        const age = now - stat.mtimeMs;\n\n        if (age > MAX_FILE_AGE_MS) {\n          rmSync(filePath);\n          deleted++;\n          bytesFreed += stat.size;\n          log('GC', `  ${C.red}DELETED${C.reset}  ${file}  (${Math.round(stat.size / 1024)}KB, age=${Math.round(age / 3600000)}h)`);\n        }\n      } catch (err) {\n        log('GC', `  ${C.yellow}SKIP${C.reset}  ${file}  (${err.message})`);\n      }\n    }\n\n    gcStats.totalScanned += scanned;\n    gcStats.totalDeleted += deleted;\n    gcStats.totalBytesFreed += bytesFreed;\n\n    if (scanned === 0) {\n      log('GC', `${C.green}Clean${C.reset}  no .bin files found`);\n    } else {\n      log('GC', `${C.green}Sweep complete${C.reset}  scanned=${scanned}  deleted=${deleted}  freed=${(bytesFreed / 1024 / 1024).toFixed(2)}MB`);\n    }\n  } catch (err) {\n    logErr(`GC sweep failed: ${err.message}`);\n  }\n}\n\n// Start the GC sweeper interval\nconst gcInterval = setInterval(garbageCollect, GARBAGE_COLLECT_INTERVAL_MS);\n// Prevent the interval from keeping the process alive if not needed\nif (gcInterval.unref) gcInterval.unref();\n\nlog('GC', `${C.cyan}Sweeper active${C.reset}  interval=${GARBAGE_COLLECT_INTERVAL_MS / 60000}min  maxAge=${MAX_FILE_AGE_MS / 3600000}h`);\n\n// ─── Fastify server ─────────────────────────────────────────────────\nconst server = fastify({\n  bodyLimit: 50 * 1024 * 1024,   // 50 MB\n  maxParamLength: 1024 * 1024,\n  disableRequestLogging: true,\n});\n\nserver.register(fastifyCors, { origin: true });\nserver.register(fastifyFormBody);\n\n// ─── Core logic ─────────────────────────────────────────────────────\n\n/**\n * Extract the System Prompt, first User message (for RAG detection),\n * and remaining user messages from an OpenAI-format\n * /v1/chat/completions request body.\n *\n * Returns:\n *   { systemPrompt, firstUserMessage, userMessages }\n */\nfunction extractSystemPrompt(body) {\n  const messages = Array.isArray(body?.messages) ? body.messages : [];\n  let systemPrompt = '';\n  const userMessages = [];\n  let firstUserMessage = null;\n\n  let systemFound = false;\n  for (const msg of messages) {\n    if (!systemFound && msg.role === 'system' && msg.content) {\n      systemPrompt = typeof msg.content === 'string'\n        ? msg.content\n        : JSON.stringify(msg.content);\n      systemFound = true;\n    } else if (msg.role === 'user' && msg.content && firstUserMessage === null) {\n      // Capture the first user message for RAG context detection\n      firstUserMessage = {\n        role: 'user',\n        content: typeof msg.content === 'string'\n          ? msg.content\n          : JSON.stringify(msg.content),\n      };\n      userMessages.push(msg);\n    } else {\n      userMessages.push(msg);\n    }\n  }\n\n  return { systemPrompt, firstUserMessage, userMessages };\n}\n\n/**\n * Compute the cache hash. If the first user message exceeds the RAG\n * threshold, it's concatenated with the system prompt so the hash\n * represents the entire static context (prefix caching).\n */\nfunction computeCacheHash(systemPrompt, firstUserMessage) {\n  let hashInput = systemPrompt;\n\n  if (firstUserMessage && typeof firstUserMessage.content === 'string') {\n    if (firstUserMessage.content.length > RAG_CONTENT_LENGTH_THRESHOLD) {\n      hashInput = `${systemPrompt}\\n\\n${firstUserMessage.content}`;\n    }\n  }\n\n  return sha256(hashInput);\n}\n\nfunction sha256(str) {\n  return createHash('sha256').update(str, 'utf8').digest('hex');\n}\n\n/**\n * POST helper using native fetch (Node ≥ 18).\n */\nasync function llamaPost(path, payload) {\n  const res = await fetch(`${LLAMA_BASE}${path}`, {\n    method: 'POST',\n    headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify(payload),\n  });\n\n  if (!res.ok) {\n    const text = await res.text().catch(() => '(no body)');\n    throw new Error(\n      `llama.cpp ${path} failed ${res.status}: ${text.slice(0, 300)}`\n    );\n  }\n\n  return res;\n}\n\nfunction sleep(ms) {\n  return new Promise((resolve) => setTimeout(resolve, ms));\n}\n\n/**\n * Save the KV cache for slot 0 to disk (background, fire-and-forget).\n */\nasync function saveKVCache(hash, model) {\n  const filename = `${hash}.bin`;\n\n  try {\n    log('SAVE', `Starting  →  ${filename}`);\n\n    const res = await llamaPost('/slots/0?action=save', { filename, model });\n\n    const bodyText = await res.text().catch(() => '');\n    let savedFile = filename;\n    try {\n      const parsed = JSON.parse(bodyText);\n      if (parsed?.path) savedFile = parsed.path;\n    } catch { /* ignore */ }\n\n    log('SAVE', `${C.green}Success${C.reset}  →  ${savedFile}  (${res.status})`);\n  } catch (err) {\n    logErr(`Save failed for ${hash}.bin: ${err.message}`);\n  }\n}\n\n/**\n * Restore the KV cache for slot 0 from disk.\n */\nasync function restoreKVCache(hash, model) {\n  const filename = `${hash}.bin`;\n\n  try {\n    log('RESTORE', `Loading  →  ${filename}`);\n\n    const res = await llamaPost('/slots/0?action=restore', { filename, model });\n\n    log('RESTORE', `${C.green}Success${C.reset}  →  ${filename}  (${res.status})`);\n  } catch (err) {\n    logErr(`Restore failed for ${hash}.bin: ${err.message}`);\n    throw err;\n  }\n}\n\n/**\n * SSE streaming proxy via hijack.\n * PRESERVED from V1 — no changes to this function.\n */\nasync function proxyStream(llamaRes) {\n  reply.hijack();\n\n  reply.raw.writeHead(llamaRes.status, {\n    'Content-Type':    'text/event-stream',\n    'Cache-Control':   'no-cache',\n    'Connection':      'keep-alive',\n    'X-Accel-Buffering': 'no',\n  });\n\n  const reader = llamaRes.body?.getReader();\n  if (reader) {\n    try {\n      while (true) {\n        const { done, value } = await reader.read();\n        if (done) break;\n        reply.raw.write(value);\n      }\n    } catch {} finally {\n      reader.releaseLock();\n      reply.raw.end();\n    }\n  } else {\n    reply.raw.end();\n  }\n}\n\n/**\n * Proxy response with custom headers (used for cache-hit path).\n * PRESERVED from V1 — inline hijack + reader/pump.\n */\nasync function proxyStreamWithHeaders(llamaRes, extraHeaders) {\n  reply.hijack();\n\n  reply.raw.writeHead(llamaRes.status, extraHeaders);\n\n  const reader = llamaRes.body?.getReader();\n  if (reader) {\n    try {\n      while (true) {\n        const { done, value } = await reader.read();\n        if (done) break;\n        reply.raw.write(value);\n      }\n    } catch {} finally {\n      reader.releaseLock();\n      reply.raw.end();\n    }\n  } else {\n    reply.raw.end();\n  }\n}\n\n// ─── Route handler ──────────────────────────────────────────────────\nserver.post('/v1/chat/completions', async (request, reply) => {\n  const body = request.body;\n  const reqId = (body?.metadata && body.metadata?.request_id)\n    || body?.id\n    || '????';\n\n  // 1. Extract system prompt + RAG context\n  const { systemPrompt, firstUserMessage, userMessages } = extractSystemPrompt(body);\n  const hash = systemPrompt ? computeCacheHash(systemPrompt, firstUserMessage) : null;\n  const model = body?.model || null;\n\n  // No system prompt → direct proxy (no caching)\n  if (!hash) {\n    log('PROXY', `No system prompt — forwarding directly (id=${reqId})`);\n\n    // Acquire a slot for uncacheable requests too\n    await acquireSlot(reqId);\n    try {\n      const llamaRes = await fetch(`${LLAMA_BASE}/v1/chat/completions`, {\n        method: 'POST',\n        headers: {\n          'Content-Type': 'application/json',\n          'Host': `${LLAMA_HOST}:${LLAMA_PORT}`,\n        },\n        body: JSON.stringify(body),\n      });\n\n      await proxyStream(llamaRes);\n    } finally {\n      releaseSlot(reqId);\n    }\n    return;\n  }\n\n  const filename = `${hash}.bin`;\n  const cacheHit = existsSync(join(CACHE_DIR, filename));\n\n  // ──────────────────────────────────────────────────────────────\n  // CACHE HIT — restore KV cache, then proxy with user messages only\n  // ──────────────────────────────────────────────────────────────\n  if (cacheHit) {\n    logHit(hash);\n\n    // Acquire a slot before proceeding\n    await acquireSlot(reqId);\n    try {\n      try {\n        await restoreKVCache(hash, model);\n        log('INFO', `KV cache restored for ${hash.slice(0, 12)}...`);\n      } catch (err) {\n        log('WARN', `Restore failed (${err.message}) — falling back to full request`);\n      }\n\n      // Proxy with only user messages (system prompt already in KV cache)\n      const proxyBody = { ...body, messages: userMessages };\n      const llamaRes = await fetch(`${LLAMA_BASE}/v1/chat/completions`, {\n        method: 'POST',\n        headers: {\n          'Content-Type': 'application/json',\n          'Host': `${LLAMA_HOST}:${LLAMA_PORT}`,\n        },\n        body: JSON.stringify(proxyBody),\n      });\n\n      await proxyStreamWithHeaders(llamaRes, {\n        'Content-Type':    'text/event-stream',\n        'Cache-Control':   'no-cache',\n        'Connection':      'keep-alive',\n        'X-Accel-Buffering': 'no',\n        'X-Cache-Status':  'HIT',\n      });\n    } finally {\n      releaseSlot(reqId);\n    }\n    return;\n  }\n\n  // ──────────────────────────────────────────────────────────────\n  // CACHE MISS — proxy full request, save KV cache after stream\n  // ──────────────────────────────────────────────────────────────\n  logMiss(hash);\n\n  // Acquire a slot before sending to llama.cpp\n  await acquireSlot(reqId);\n  try {\n    const llamaRes = await fetch(`${LLAMA_BASE}/v1/chat/completions`, {\n      method: 'POST',\n      headers: {\n        'Content-Type': 'application/json',\n        'Host': `${LLAMA_HOST}:${LLAMA_PORT}`,\n      },\n      body: JSON.stringify(body),\n    });\n\n    if (!llamaRes.ok) {\n      const errText = await llamaRes.text().catch(() => '');\n      logErr(`llama.cpp returned ${llamaRes.status}: ${errText.slice(0, 500)}`);\n      throw new Error(`llama.cpp returned ${llamaRes.status}: ${errText.slice(0, 300)}`);\n    }\n\n    reply.hijack();\n    reply.raw.writeHead(llamaRes.status, {\n      'Content-Type':    'text/event-stream',\n      'Cache-Control':   'no-cache',\n      'Connection':      'keep-alive',\n      'X-Accel-Buffering': 'no',\n      'X-Cache-Status':  'MISS',\n    });\n\n    const reader = llamaRes.body?.getReader();\n\n    if (reader) {\n      // Pump the stream to the client\n      const pump = async () => {\n        try {\n          while (true) {\n            const { done, value } = await reader.read();\n            if (done) break;\n            reply.raw.write(value);\n          }\n        } catch {} finally {\n          reader.releaseLock();\n          reply.raw.end();\n        }\n      };\n\n      // Save KV cache after stream is fully consumed (background)\n      (async () => {\n        try {\n          await pump();\n        } finally {\n          await saveKVCache(hash, model);\n        }\n      })();\n    } else {\n      reply.raw.end();\n      setImmediate(() => saveKVCache(hash, model));\n    }\n  } finally {\n    releaseSlot(reqId);\n  }\n\n  return;\n});\n\n// ─── Error handler ──────────────────────────────────────────────────\nserver.setErrorHandler((error, request, reply) => {\n  logErr(`${error.message}  [${request.id || 'no-id'}]`);\n  if (!reply.sent) {\n    reply.status(500).send({\n      error: {\n        message: error.message,\n        type: 'internal_error',\n      },\n    });\n  }\n});\n\n// ─── Startup ────────────────────────────────────────────────────────\nconst start = async () => {\n  try {\n    await server.listen({ port: PORT, host: '0.0.0.0' });\n\n    console.log(`\n${C.bold}${C.cyan}╔═══════════════════════════════════════════════════════════╗${C.reset}\n${C.bold}${C.cyan}║${C.reset}  ${C.bold}${C.green}KV Cache Semantic Router — Enterprise V2${C.reset}              ${C.bold}${C.cyan}║${C.reset}\n${C.bold}${C.cyan}║${C.reset}  Listening on  ${C.bold}http://0.0.0.0:${PORT}${C.reset}                      ${C.bold}${C.cyan}║${C.reset}\n${C.bold}${C.cyan}║${C.reset}  Proxy target  ${C.bold}${LLAMA_BASE}${C.reset}                                    ${C.bold}${C.cyan}║${C.reset}\n${C.bold}${C.cyan}║${C.reset}  Cache dir     ${C.bold}${CACHE_DIR}${C.reset}                                    ${C.bold}${C.cyan}║${C.reset}\n${C.bold}${C.cyan}║${C.reset}  Max slots     ${C.bold}${MAX_CONCURRENT_SLOTS}${C.reset}                      ${C.bold}${C.cyan}║${C.reset}\n${C.bold}${C.cyan}║${C.reset}  GC interval   ${C.bold}${GARBAGE_COLLECT_INTERVAL_MS / 60000}min${C.reset}  maxAge ${MAX_FILE_AGE_MS / 3600000}h${C.reset}    ${C.bold}${C.cyan}║${C.reset}\n${C.bold}${C.cyan}║${C.reset}  RAG threshold ${C.bold}${RAG_CONTENT_LENGTH_THRESHOLD} chars${C.reset}                    ${C.bold}${C.cyan}║${C.reset}\n${C.bold}${C.cyan}╚═══════════════════════════════════════════════════════════╝${C.reset}\n`);\n  } catch (err) {\n    logErr(`Cannot start server: ${err.message}`);\n    process.exit(1);\n  }\n};\n\nstart();\n",
  "file_path": "/home/daniel/qwencode/server.js"
}
```

**Affected Files:**
- `/home/daniel/qwencode/server.js`

**Output:**

*Diff for `server.js`:*
```diff
#!/usr/bin/env node
/**
 * KV Cache Semantic Router — Enterprise V2
 * =========================================
 * Inverse API Gateway for llama.cpp with:
 *   • Dynamic Slot Load Balancing (FIFO concurrency queue)
 *   • LRU Garbage Collector (RAMDisk sweeper)
 *   • Advanced RAG-aware Prefix Hashing
 *   • reply.hijack() SSE streaming (preserved)
 *
 * Usage:
 *   node server.js
 *
 * Listens on :8080, proxies to llama.cpp at :11434.
 */

'use strict';

// ─── Dependencies ───────────────────────────────────────────────────
// npm install fastify @fastify/cors @fastify/formbody
// (fetch is native in Node ≥ 18)

const { createHash } = require('crypto');
const { existsSync, mkdirSync, statSync, readdirSync, rmSync } = require('fs');
const { join, extname } = require('path');
const fastify = require('fastify');
const fastifyCors = require('@fastify/cors');
const fastifyFormBody = require('@fastify/formbody');

// ─── Configuration ──────────────────────────────────────────────────
const PORT = 8080;
const LLAMA_HOST = '127.0.0.1';
const LLAMA_PORT = 11434;
const LLAMA_BASE = `http://${LLAMA_HOST}:${LLAMA_PORT}`;
const CACHE_DIR = join('/home/daniel', 'Models', 'slot_cache');

// Dynamic Slot Load Balancing
const MAX_CONCURRENT_SLOTS = 3;

// LRU Garbage Collector
const GARBAGE_COLLECT_INTERVAL_MS = 5 * 60 * 1000;  // 5 minutes
const MAX_FILE_AGE_MS = 12 * 60 * 60 * 1000;        // 12 hours

// RAG-aware prefix caching
const RAG_CONTENT_LENGTH_THRESHOLD = 1000;

// ─── Helpers ────────────────────────────────────────────────────────

// ANSI colour helpers for terminal output
const C = {
  reset:  '\x1b[0m',
  bold:   '\x1b[1m',
  grey:   '\x1b[90m',
  green:  '\x1b[32m',
  yellow: '\x1b[33m',
  red:    '\x1b[31m',
  cyan:   '\x1b[36m',
  blue:   '\x1b[34m',
};

function log(tag, msg) {
  console.log(`${C.grey}${new Date().toISOString().slice(11, 23)}${C.reset}  ${C.bold}[${tag}]${C.reset} ${msg}`);
}

function logHit(hash) {
  console.log(
    `${C.green}${C.bold}■ CACHE HIT${C.reset}  slot  →  ${hash}.bin  (restoring from disk)`
  );
}

function logMiss(hash) {
  console.log(
    `${C.yellow}${C.bold}■ CACHE MISS${C.reset}  slot  →  ${hash}.bin  (will save after response)`
  );
}

function logWarn(msg) {
  console.log(
    `${C.yellow}${C.bold}⚠ WARN${C.reset}    ${msg}`
  );
}

function logErr(msg) {
  console.error(`${C.red}${C.bold}✖ ERROR${C.reset}   ${msg}`);
}

// ─── Ensure cache directory exists ──────────────────────────────────
if (!existsSync(CACHE_DIR)) {
  mkdirSync(CACHE_DIR, { recursive: true });
  log('INIT', `Created cache directory: ${CACHE_DIR}`);
}

// ─── 1. Dynamic Slot Load Balancing (Concurrency Queue) ─────────────
//
// Promise-based FIFO semaphore. Non-blocking — all waiting happens
// via microtask resolution, never blocks the Node event loop.
//
//   acquire() → returns a Promise that resolves when a slot opens
//   release() → frees a slot, wakes the next waiter in line
//
let currentSlots = 0;
const slotQueue = []; // FIFO queue of { resolve, reject, reqId }

/**
 * Acquire a concurrency slot. If all MAX_CONCURRENT_SLOTS are in
 * use, the caller's promise is queued and resolved later when a
 * slot frees up.
 */
function acquireSlot(reqId) {
  return new Promise((resolve, reject) => {
    if (currentSlots < MAX_CONCURRENT_SLOTS) {
      currentSlots++;
      log('SLOT', `${C.green}ACQUIRED${C.reset}  (${currentSlots}/${MAX_CONCURRENT_SLOTS})  ${reqId}`);
      resolve();
    } else {
      log('SLOT', `${C.yellow}QUEUED${C.reset}  queue=${slotQueue.length + 1}  (${currentSlots}/${MAX_CONCURRENT_SLOTS})  ${reqId}`);
      slotQueue.push({ resolve, reject, reqId });
    }
  });
}

/**
 * Release a concurrency slot. If there are waiters in the FIFO
 * queue, the next one gets the slot immediately. Otherwise, just
 * decrement the counter.
 */
function releaseSlot(reqId) {
  if (slotQueue.length > 0) {
    const { resolve } = slotQueue.shift();
    currentSlots++;
    log('SLOT', `${C.green}PASSED${C.reset}  queue=${slotQueue.length}  (${currentSlots}/${MAX_CONCURRENT_SLOTS})  ${reqId}`);
    resolve();
  } else {
    currentSlots--;
    log('SLOT', `${C.green}RELEASED${C.reset}  (${currentSlots}/${MAX_CONCURRENT_SLOTS})  ${reqId}`);
  }
}

// ─── 2. LRU Garbage Collector (Background Sweeper) ─────────────────

let gcStats = { totalScanned: 0, totalDeleted: 0, totalBytesFreed: 0 };

function garbageCollect() {
  log('GC', `${C.bold}Starting sweep${C.reset}  (max age=${(MAX_FILE_AGE_MS / 3600000).toFixed(0)}h)`);

  try {
    const files = readdirSync(CACHE_DIR).filter(f => extname(f) === '.bin');
    const now = Date.now();
    let scanned = 0;
    let deleted = 0;
    let bytesFreed = 0;

    for (const file of files) {
      scanned++;
      const filePath = join(CACHE_DIR, file);

      try {
        const stat = statSync(filePath);
        const age = now - stat.mtimeMs;

        if (age > MAX_FILE_AGE_MS) {
          rmSync(filePath);
          deleted++;
          bytesFreed += stat.size;
          log('GC', `  ${C.red}DELETED${C.reset}  ${file}  (${Math.round(stat.size / 1024)}KB, age=${Math.round(age / 3600000)}h)`);
        }
      } catch (err) {
        log('GC', `  ${C.yellow}SKIP${C.reset}  ${file}  (${err.message})`);
      }
    }

    gcStats.totalScanned += scanned;
    gcStats.totalDeleted += deleted;
    gcStats.totalBytesFreed += bytesFreed;

    if (scanned === 0) {
      log('GC', `${C.green}Clean${C.reset}  no .bin files found`);
    } else {
      log('GC', `${C.green}Sweep complete${C.reset}  scanned=${scanned}  deleted=${deleted}  freed=${(bytesFreed / 1024 / 1024).toFixed(2)}MB`);
    }
  } catch (err) {
    logErr(`GC sweep failed: ${err.message}`);
  }
}

// Start the GC sweeper interval
const gcInterval = setInterval(garbageCollect, GARBAGE_COLLECT_INTERVAL_MS);
// Prevent the interval from keeping the process alive if not needed
if (gcInterval.unref) gcInterval.unref();

log('GC', `${C.cyan}Sweeper active${C.reset}  interval=${GARBAGE_COLLECT_INTERVAL_MS / 60000}min  maxAge=${MAX_FILE_AGE_MS / 3600000}h`);

// ─── Fastify server ─────────────────────────────────────────────────
const server = fastify({
  bodyLimit: 50 * 1024 * 1024,   // 50 MB
  maxParamLength: 1024 * 1024,
  disableRequestLogging: true,
});

server.register(fastifyCors, { origin: true });
server.register(fastifyFormBody);

// ─── Core logic ─────────────────────────────────────────────────────

/**
 * Extract the System Prompt, first User message (for RAG detection),
 * and remaining user messages from an OpenAI-format
 * /v1/chat/completions request body.
 *
 * Returns:
 *   { systemPrompt, firstUserMessage, userMessages }
 */
function extractSystemPrompt(body) {
  const messages = Array.isArray(body?.messages) ? body.messages : [];
  let systemPrompt = '';
  const userMessages = [];
  let firstUserMessage = null;

  let systemFound = false;
  for (const msg of messages) {
    if (!systemFound && msg.role === 'system' && msg.content) {
      systemPrompt = typeof msg.content === 'string'
        ? msg.content
        : JSON.stringify(msg.content);
      systemFound = true;
    } else if (msg.role === 'user' && msg.content && firstUserMessage === null) {
      // Capture the first user message for RAG context detection
      firstUserMessage = {
        role: 'user',
        content: typeof msg.content === 'string'
          ? msg.content
          : JSON.stringify(msg.content),
      };
      userMessages.push(msg);
    } else {
      userMessages.push(msg);
    }
  }

  return { systemPrompt, firstUserMessage, userMessages };
}

/**
 * Compute the cache hash. If the first user message exceeds the RAG
 * threshold, it's concatenated with the system prompt so the hash
 * represents the entire static context (prefix caching).
 */
function computeCacheHash(systemPrompt, firstUserMessage) {
  let hashInput = systemPrompt;

  if (firstUserMessage && typeof firstUserMessage.content === 'string') {
    if (firstUserMessage.content.length > RAG_CONTENT_LENGTH_THRESHOLD) {
      hashInput = `${systemPrompt}\n\n${firstUserMessage.content}`;
    }
  }

  return sha256(hashInput);
}

function sha256(str) {
  return createHash('sha256').update(str, 'utf8').digest('hex');
}

/**
 * POST helper using native fetch (Node ≥ 18).
 */
async function llamaPost(path, payload) {
  const res = await fetch(`${LLAMA_BASE}${path}`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(payload),
  });

  if (!res.ok) {
    const text = await res.text().catch(() => '(no body)');
    throw new Error(
      `llama.cpp ${path} failed ${res.status}: ${text.slice(0, 300)}`
    );
  }

  return res;
}

function sleep(ms) {
  return new Promise((resolve) => setTimeout(resolve, ms));
}

/**
 * Save the KV cache for slot 0 to disk (background, fire-and-forget).
 */
async function saveKVCache(hash, model) {
  const filename = `${hash}.bin`;

  try {
    log('SAVE', `Starting  →  ${filename}`);

    const res = await llamaPost('/slots/0?action=save', { filename, model });

    const bodyText = await res.text().catch(() => '');
    let savedFile = filename;
    try {
      const parsed = JSON.parse(bodyText);
      if (parsed?.path) savedFile = parsed.path;
    } catch { /* ignore */ }

    log('SAVE', `${C.green}Success${C.reset}  →  ${savedFile}  (${res.status})`);
  } catch (err) {
    logErr(`Save failed for ${hash}.bin: ${err.message}`);
  }
}

/**
 * Restore the KV cache for slot 0 from disk.
 */
async function restoreKVCache(hash, model) {
  const filename = `${hash}.bin`;

  try {
    log('RESTORE', `Loading  →  ${filename}`);

    const res = await llamaPost('/slots/0?action=restore', { filename, model });

    log('RESTORE', `${C.green}Success${C.reset}  →  ${filename}  (${res.status})`);
  } catch (err) {
    logErr(`Restore failed for ${hash}.bin: ${err.message}`);
    throw err;
  }
}

/**
 * SSE streaming proxy via hijack.
 * PRESERVED from V1 — no changes to this function.
 */
async function proxyStream(llamaRes) {
  reply.hijack();

  reply.raw.writeHead(llamaRes.status, {
    'Content-Type':    'text/event-stream',
    'Cache-Control':   'no-cache',
    'Connection':      'keep-alive',
    'X-Accel-Buffering': 'no',
  });

  const reader = llamaRes.body?.getReader();
  if (reader) {
    try {
      while (true) {
        const { done, value } = await reader.read();
        if (done) break;
        reply.raw.write(value);
      }
    } catch {} finally {
      reader.releaseLock();
      reply.raw.end();
    }
  } else {
    reply.raw.end();
  }
}

/**
 * Proxy response with custom headers (used for cache-hit path).
 * PRESERVED from V1 — inline hijack + reader/pump.
 */
async function proxyStreamWithHeaders(llamaRes, extraHeaders) {
  reply.hijack();

  reply.raw.writeHead(llamaRes.status, extraHeaders);

  const reader = llamaRes.body?.getReader();
  if (reader) {
    try {
      while (true) {
        const { done, value } = await reader.read();
        if (done) break;
        reply.raw.write(value);
      }
    } catch {} finally {
      reader.releaseLock();
      reply.raw.end();
    }
  } else {
    reply.raw.end();
  }
}

// ─── Route handler ──────────────────────────────────────────────────
server.post('/v1/chat/completions', async (request, reply) => {
  const body = request.body;
  const reqId = (body?.metadata && body.metadata?.request_id)
    || body?.id
    || '????';

  // 1. Extract system prompt + RAG context
  const { systemPrompt, firstUserMessage, userMessages } = extractSystemPrompt(body);
  const hash = systemPrompt ? computeCacheHash(systemPrompt, firstUserMessage) : null;
  const model = body?.model || null;

  // No system prompt → direct proxy (no caching)
  if (!hash) {
    log('PROXY', `No system prompt — forwarding directly (id=${reqId})`);

    // Acquire a slot for uncacheable requests too
    await acquireSlot(reqId);
    try {
      const llamaRes = await fetch(`${LLAMA_BASE}/v1/chat/completions`, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'Host': `${LLAMA_HOST}:${LLAMA_PORT}`,
        },
        body: JSON.stringify(body),
      });

      await proxyStream(llamaRes);
    } finally {
      releaseSlot(reqId);
    }
    return;
  }

  const filename = `${hash}.bin`;
  const cacheHit = existsSync(join(CACHE_DIR, filename));

  // ──────────────────────────────────────────────────────────────
  // CACHE HIT — restore KV cache, then proxy with user messages only
  // ──────────────────────────────────────────────────────────────
  if (cacheHit) {
    logHit(hash);

    // Acquire a slot before proceeding
    await acquireSlot(reqId);
    try {
      try {
        await restoreKVCache(hash, model);
        log('INFO', `KV cache restored for ${hash.slice(0, 12)}...`);
      } catch (err) {
        log('WARN', `Restore failed (${err.message}) — falling back to full request`);
      }

      // Proxy with only user messages (system prompt already in KV cache)
      const proxyBody = { ...body, messages: userMessages };
      const llamaRes = await fetch(`${LLAMA_BASE}/v1/chat/completions`, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'Host': `${LLAMA_HOST}:${LLAMA_PORT}`,
        },
        body: JSON.stringify(proxyBody),
      });

      await proxyStreamWithHeaders(llamaRes, {
        'Content-Type':    'text/event-stream',
        'Cache-Control':   'no-cache',
        'Connection':      'keep-alive',
        'X-Accel-Buffering': 'no',
        'X-Cache-Status':  'HIT',
      });
    } finally {
      releaseSlot(reqId);
    }
    return;
  }

  // ──────────────────────────────────────────────────────────────
  // CACHE MISS — proxy full request, save KV cache after stream
  // ──────────────────────────────────────────────────────────────
  logMiss(hash);

  // Acquire a slot before sending to llama.cpp
  await acquireSlot(reqId);
  try {
    const llamaRes = await fetch(`${LLAMA_BASE}/v1/chat/completions`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Host': `${LLAMA_HOST}:${LLAMA_PORT}`,
      },
      body: JSON.stringify(body),
    });

    if (!llamaRes.ok) {
      const errText = await llamaRes.text().catch(() => '');
      logErr(`llama.cpp returned ${llamaRes.status}: ${errText.slice(0, 500)}`);
      throw new Error(`llama.cpp returned ${llamaRes.status}: ${errText.slice(0, 300)}`);
    }

    reply.hijack();
    reply.raw.writeHead(llamaRes.status, {
      'Content-Type':    'text/event-stream',
      'Cache-Control':   'no-cache',
      'Connection':      'keep-alive',
      'X-Accel-Buffering': 'no',
      'X-Cache-Status':  'MISS',
    });

    const reader = llamaRes.body?.getReader();

    if (reader) {
      // Pump the stream to the client
      const pump = async () => {
        try {
          while (true) {
            const { done, value } = await reader.read();
            if (done) break;
            reply.raw.write(value);
          }
        } catch {} finally {
          reader.releaseLock();
          reply.raw.end();
        }
      };

      // Save KV cache after stream is fully consumed (background)
      (async () => {
        try {
          await pump();
        } finally {
          await saveKVCache(hash, model);
        }
      })();
    } else {
      reply.raw.end();
      setImmediate(() => saveKVCache(hash, model));
    }
  } finally {
    releaseSlot(reqId);
  }

  return;
});

// ─── Error handler ──────────────────────────────────────────────────
server.setErrorHandler((error, request, reply) => {
  logErr(`${error.message}  [${request.id || 'no-id'}]`);
  if (!reply.sent) {
    reply.status(500).send({
      error: {
        message: error.message,
        type: 'internal_error',
      },
    });
  }
});

// ─── Startup ────────────────────────────────────────────────────────
const start = async () => {
  try {
    await server.listen({ port: PORT, host: '0.0.0.0' });

    console.log(`
${C.bold}${C.cyan}╔═══════════════════════════════════════════════════════════╗${C.reset}
${C.bold}${C.cyan}║${C.reset}  ${C.bold}${C.green}KV Cache Semantic Router — Enterprise V2${C.reset}              ${C.bold}${C.cyan}║${C.reset}
${C.bold}${C.cyan}║${C.reset}  Listening on  ${C.bold}http://0.0.0.0:${PORT}${C.reset}                      ${C.bold}${C.cyan}║${C.reset}
${C.bold}${C.cyan}║${C.reset}  Proxy target  ${C.bold}${LLAMA_BASE}${C.reset}                                    ${C.bold}${C.cyan}║${C.reset}
${C.bold}${C.cyan}║${C.reset}  Cache dir     ${C.bold}${CACHE_DIR}${C.reset}                                    ${C.bold}${C.cyan}║${C.reset}
${C.bold}${C.cyan}║${C.reset}  Max slots     ${C.bold}${MAX_CONCURRENT_SLOTS}${C.reset}                      ${C.bold}${C.cyan}║${C.reset}
${C.bold}${C.cyan}║${C.reset}  GC interval   ${C.bold}${GARBAGE_COLLECT_INTERVAL_MS / 60000}min${C.reset}  maxAge ${MAX_FILE_AGE_MS / 3600000}h${C.reset}    ${C.bold}${C.cyan}║${C.reset}
${C.bold}${C.cyan}║${C.reset}  RAG threshold ${C.bold}${RAG_CONTENT_LENGTH_THRESHOLD} chars${C.reset}                    ${C.bold}${C.cyan}║${C.reset}
${C.bold}${C.cyan}╚═══════════════════════════════════════════════════════════╝${C.reset}
`);
  } catch (err) {
    logErr(`Cannot start server: ${err.message}`);
    process.exit(1);
  }
};

start();

```


## Assistant

Good, the file has been written. Let me verify it by reading it back and checking for any syntax issues. Then I should try to run a syntax check.


### Tool: TodoWrite
**Status**: completed

**Output:**
```
- [x] Implement Dynamic Slot Load Balancing (Semaphore/Queue)
- [x] Implement LRU Garbage Collector (setInterval sweeper)
- [x] Implement Advanced RAG-aware Hashing (prefix caching)
- [x] Preserve existing hijack/SSE streaming logic
- [x] Write complete refactored server.js
- [-] Verify syntax and run lint check
```


## Assistant

Let me verify the syntax is correct by running node --check on the file.


### Tool: Shell: node --check /home/daniel/qwencode/server.js (Syntax-check server.js)
**Status**: completed

**Input:**
```json
{
  "command": "node --check /home/daniel/qwencode/server.js",
  "description": "Syntax-check server.js"
}
```

**Output:**
```
Command: node --check /home/daniel/qwencode/server.js
Directory: (root)
Output: (empty)
Error: (none)
Exit Code: 0
Signal: 0
Process Group PGID: 11280
```


## Assistant

Syntax check passed. Let me also check if there's a lint script in package.json.


### Tool: ReadFile: package.json
**Status**: completed

**Input:**
```json
{
  "file_path": "/home/daniel/qwencode/package.json"
}
```

**Affected Files:**
- `/home/daniel/qwencode/package.json`

**Output:**
```json
{
  "name": "kv-cache-semantic-router",
  "version": "1.0.0",
  "description": "Inverse API Gateway for llama.cpp — caches KV state by hashing System Prompts",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/formbody": "^7.4.0",
    "fastify": "^4"
  }
}

```


## Assistant

No lint script configured. The syntax check passed. Let me mark the verification as complete and summarize the changes.


### Tool: TodoWrite
**Status**: completed

**Output:**
```
- [x] Implement Dynamic Slot Load Balancing (Semaphore/Queue)
- [x] Implement LRU Garbage Collector (setInterval sweeper)
- [x] Implement Advanced RAG-aware Hashing (prefix caching)
- [x] Preserve existing hijack/SSE streaming logic
- [x] Write complete refactored server.js
- [x] Verify syntax and run lint check
```


## Assistant

All tasks completed. Let me provide a concise summary of the changes.


## Assistant

Syntax check passed. Here's the summary of what changed in `server.js`:

### 1. Dynamic Slot Load Balancing
- **`acquireSlot(reqId)`** / **`releaseSlot(reqId)`** — promise-based FIFO semaphore with `MAX_CONCURRENT_SLOTS = 3`. All waiting happens via microtask resolution, zero event-loop blocking.
- Every request path (no-cache proxy, cache-hit, cache-miss) acquires a slot before contacting llama.cpp and releases it in a `finally` block.
- Console logs show `ACQUIRED`, `QUEUED`, `PASSED`, `RELEASED` with queue depth and slot count.

### 2. LRU Garbage Collector
- `setInterval(garbageCollect, 5min)` scans `CACHE_DIR` for `.bin` files.
- Uses `statSync` to check `mtimeMs`, deletes files older than **12 hours**.
- Logs each deletion with file size and age. Accumulates totals (`scanned`, `deleted`, `freedMB`).
- Interval is `unref()`'d so it won't prevent process exit.

### 3. Advanced RAG-aware Prefix Hashing
- `extractSystemPrompt()` now returns `{ systemPrompt, firstUserMessage, userMessages }`.
- `computeCacheHash()` checks if the first user message content exceeds **1000 chars**. If so, concatenates `systemPrompt + '\n\n' + firstUserContent` before hashing — maximizing cache hits for repeated RAG contexts.

### 4. Preserved
- `reply.hijack()` SSE streaming untouched (extracted into `proxyStream()` and `proxyStreamWithHeaders()` helpers for DRY).
- Native `fetch`, `FST_ERR_REP_ALREADY_SENT` fix, ANSI console colors, `@fastify/formbody` — all unchanged.

